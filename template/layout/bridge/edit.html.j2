{% from macro('header') import render_header with context %}
{% from macro('form') import render_form %}

{% extends layout('base') %}

{% block header %}
    {{ render_header(
        header.icon,
        header.title,
        subtitle='Editar'
    ) }}
{% endblock %}

{% block main %}
    {{ render_form(
        form,
        action=view(module ~ ':put', id=model.id),
        href=view(module ~ ':index', primary_id=model.primary_id),
    ) }}
{% endblock %}